services:
  - type: web
    name: dh-km-assistant
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 10000
